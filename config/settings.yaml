# Modular RAG MCP Server 配置文件
# 主配置文件，定义 LLM、Embedding、VectorStore 等组件的配置

# LLM 配置
llm:
  provider: qwen  # azure | openai | ollama | deepseek | dashscope | qwen
  model: qwen-turbo  # qwen-turbo | qwen-plus | qwen-max
  # Azure 配置
  azure_endpoint: ""  # 从环境变量读取: ${AZURE_OPENAI_ENDPOINT}
  azure_api_key: ""   # 从环境变量读取: ${AZURE_OPENAI_API_KEY}
  azure_api_version: "2024-02-15-preview"
  deployment_name: ""  # Azure deployment name
  # OpenAI 配置
  openai_api_key: ""   # 从环境变量读取: ${OPENAI_API_KEY}
  # Ollama 配置
  ollama_base_url: "http://localhost:11434"
  # DeepSeek 配置
  deepseek_api_key: ""  # 从环境变量读取: ${DEEPSEEK_API_KEY}
  deepseek_base_url: "https://api.deepseek.com"
  # DashScope/Qwen 配置（可在 settings.local.yaml 中覆盖）
  dashscope_api_key: ""
  dashscope_base_url: "https://dashscope.aliyuncs.com"

# Vision LLM 配置 (图片描述生成)
vision_llm:
  provider: qwen  # azure | dashscope | qwen (Qwen-VL)
  model: qwen-vl-max  # qwen-vl-max | qwen-vl-plus
  azure_endpoint: ""
  azure_api_key: ""
  azure_api_version: "2024-02-15-preview"
  deployment_name: ""
  # DashScope (Qwen-VL) 配置（可在 settings.local.yaml 中覆盖）
  dashscope_api_key: ""
  dashscope_base_url: "https://dashscope.aliyuncs.com"

# Embedding 配置
embedding:
  provider: qwen  # openai | ollama | local | dashscope | qwen
  model: text-embedding-v1  # text-embedding-v1 | text-embedding-v2
  openai_api_key: ""
  # DashScope/Qwen Embedding 配置（可在 settings.local.yaml 中覆盖）
  dashscope_api_key: ""
  dashscope_base_url: "https://dashscope.aliyuncs.com"
  # 本地模型配置
  local_model_path: ""
  device: "cpu"  # cpu | cuda

# 向量存储配置（统一存储：按 backend 使用对应配置）
# 各 backend 在其自身容器内存储 chunk、向量、稀疏索引、图片
vector_store:
  backend: sqlite  # chroma | qdrant | pinecone | sqlite（推荐 sqlite 统一存储）
  persist_path: "./data/db/qdrant"  # chroma | qdrant 时生效
  collection_name: "cslearning"
  # backend=sqlite 时生效
  sqlite_path: "./data/db/rag.sqlite"
  embedding_dim: 1536  # 与 embedding 模型一致，运行 scripts/embedding_dim.py 可查询
  # backend=qdrant 时生效
  qdrant_url: ""
  qdrant_api_key: ""  # 云服务时从 ${QDRANT_API_KEY} 读取

# 检索配置
retrieval:
  sparse_backend: fts5  # backend=sqlite 时必须为 fts5（FTS5 与 chunk 同库）
  fusion_algorithm: rrf  # rrf | weighted_sum
  top_k_dense: 30
  top_k_sparse: 30
  top_k_final: 15
  aggregate_by_parent: false       # 是否启用父子聚合（heading 切分场景）
  parent_id_metadata_key: parent_id
  parent_score_strategy: sum       # sum | count
  parent_aggregate_top_m: 30       # 交给聚合器前保留的子块候选数

# 重排配置
rerank:
  backend: cross_encoder # none | cross_encoder | llm
  model: "./model"
  top_m: 10
  timeout_seconds: 10

# 评估配置
evaluation:
  backends: [ragas, custom]
  golden_test_set: "./tests/fixtures/golden_test_set.json"

# 可观测性配置
observability:
  enabled: true
  logging:
    log_file: "./logs/traces.jsonl"
    log_level: INFO  # DEBUG | INFO | WARNING
  detail_level: standard  # minimal | standard | verbose
  dashboard:
    enabled: true
    port: 8501

# MinerU 云端 API 配置（ingest_document_mineru 工具使用，可在 settings.local.yaml 中覆盖）
mineru:
  api_token: ""  # 从 settings.local.yaml 或 ${MINERU_API_TOKEN} 读取
  model_version: vlm
  poll_interval_seconds: 5
  poll_timeout_seconds: 600

# Ingestion Pipeline 配置
ingestion:
  chunk_size: 512
  chunk_overlap: 50
  enable_llm_refinement: false  # 是否启用 LLM 重写 Chunk
  enable_metadata_enrichment: false
  enable_image_captioning: true
  batch_size: 25  # DashScope text-embedding 单批上限 25
  splitter_strategy: recursive    # recursive | heading（heading 用于父子索引场景）
  heading_split_level: 3        # 在第几级标题处切块（3 = ###）
  heading_parent_level: 2       # 哪一级标题作为父（2 = ##）
  enable_boundary_repair: false  # 是否启用块边界语义修复
  boundary_repair_mode: rule     # rule（规则拼接，零成本） | llm（LLM 重写，更准但需 API）
